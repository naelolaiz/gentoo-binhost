# Binhost-specific package masks
# Mask packages that are known to be problematic in the CI environment
# or that should not be served from the binhost

# Mask libclc-22+ to avoid slot conflict with mesa_clc which requires =libclc-21*
>=llvm-core/libclc-22
